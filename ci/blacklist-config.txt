# Configs to skip building
# Add config names here (without .mk extension), one per line
# Lines starting with # are ignored

# Skipped because it contains no images
blank

# Skipped because it contains an image with a proprietary license 
c64

# Skipped because we're not currently building firmware with sets in - due to HW_REV=f requirement
set-c64
set-vic20-pal
